<?xml version="1.0"?>
<skin name="default_scaler_3_skin" version="0.0">

	<!-- shared variables -->
	
	<variables>
		<variable key="asset" value="habbo_skin_ubuntu_png" type="String" />
	</variables>
	
	<!-- the following maps layouts for render states -->

	<states>
		<state name="default" layout="scaler_3" template="default_scaler_3" />
	</states>

	<!-- templates defines regions in bitmap asset -->
	
	<templates>
		<template name="default_scaler_3" asset="$asset">
			<entities>
				<entity name="scaler" type="bitmap"><region><Rectangle x="120" y= "100" width="20" height="20" /></region></entity>
			</entities>
		</template>
	</templates>
	
	<!-- markup for layout drawer -->
	
	<layouts>
		<layout name="scaler_3" transparent="true">
			<entities>
				<entity name="scaler" id="0">
					<region><Rectangle x="0" y="0" width="20" height="20" /></region>
					<scale horizontal="fixed" vertical="fixed" />
				</entity>
			</entities>
		</layout>
	</layouts>
</skin>